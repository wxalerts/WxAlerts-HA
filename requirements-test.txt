# Test-only dependencies. pytest-homeassistant-custom-component pins the
# Home Assistant core it was built against, which is what the entity tests
# run inside.
pytest-homeassistant-custom-component
aiomqtt>=2.3.0
pytest-cov
