[pytest]
testpaths = tests
asyncio_mode = auto
# Live tests dial the production broker; run them with `pytest -m live`.
addopts = -q --strict-markers -m "not live"
markers =
    live: hits wss://mqtt.wxalerts.org — network required, weather-dependent
filterwarnings =
    ignore::DeprecationWarning
